{
  "name": "@localwebauthn/channels-core",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Shared delivery core for the channel examples: fixed templates, destination validation, fetch-based Twilio and Resend senders",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
